# AGENTS.md

Guidance for agents working in this repository.

## Agent skills

### Issue tracker

GitHub Issues on the fork `gj513174036/dsh-web-fetch-playwright` — every writing command passes `-R`, because bare `gh` resolves to `upstream`. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, label strings unchanged (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`); all present on the tracker. See `docs/agents/triage-labels.md`.

### Domain docs

single-context — one root `CONTEXT.md` plus `docs/adr/`. See `docs/agents/domain.md`.
